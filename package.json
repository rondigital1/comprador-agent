{
  "name": "comprador",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "pnpm": {
    "overrides": {
      "google-auth-library": "10.5.0"
    }
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "db:generate": "pnpm --filter @comprador/database db:generate",
    "db:migrate": "pnpm --filter @comprador/database db:migrate",
    "db:studio": "pnpm --filter @comprador/database db:studio",
    "dev": "pnpm --parallel --stream --filter @comprador/web --filter @comprador/worker dev",
    "dev:web": "pnpm --filter @comprador/web dev",
    "dev:worker": "pnpm --filter @comprador/worker dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "pnpm -r --if-present lint",
    "test": "vitest run",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "devDependencies": {
    "dotenv": "17.4.2",
    "prettier": "3.9.6",
    "tsx": "4.23.1",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  }
}
